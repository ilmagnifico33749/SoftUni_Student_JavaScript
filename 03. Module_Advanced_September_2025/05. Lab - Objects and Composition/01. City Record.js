function cityRecords(name, population, treasury) {
    const city = {};
    city.name = name;
    city.population = population;
    city.treasury = treasury;

    return city;
}

cityRecords('Tortuga', 7000, 15000);
cityRecords('Santo Domingo', 12000, 23500);
